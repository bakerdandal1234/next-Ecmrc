import React from 'react';
export const metadata = {
    title: "card page",
    description: "description to card page",
  };
const Page = () => {
    return (
        <div>
            <h1>Card page</h1>
        </div>
    );
}

export default Page;
