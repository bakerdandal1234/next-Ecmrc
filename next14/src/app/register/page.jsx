import React from 'react';
export const metadata = {
    title: "register page",
    description: "description to register page",
  };
const Page = () => {
    return (
        <div>
            <h1>register page</h1>
        </div>
    );
}

export default Page;
