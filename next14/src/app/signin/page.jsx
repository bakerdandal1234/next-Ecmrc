import React from 'react';
export const metadata = {
    title: "sign-in page",
    description: "description to sign-in page",
  };
const Page = () => {
    return (
        <div>
            <h1>sign in page</h1>
        </div>
    );
}

export default Page;
